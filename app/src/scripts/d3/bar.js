var Bar = function(data) {
    this.plot(data);
};

Bar.prototype.crunch = function (data) {
    var dates = [];
    var count = 0;
    for (d in data.byDate) {
        var obj = {};
        obj.x = k;
        obj.y = 0;
    }
};

Bar.prototype.plot = function (data) {
    var dates = this.crunch(data);




};

module.exports = Bar;